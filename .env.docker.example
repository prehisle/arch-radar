# ==========================================
# Docker Compose 环境（本地/服务器容器部署）
# ==========================================
# 使用方法：cp .env.docker.example .env
# 适用场景：使用 docker-compose.yml 部署，MySQL/Redis 运行在容器中
# 注意：Sealos/K8s 生产环境建议使用 ConfigMap/Secret，不依赖此文件

# ---- MySQL 容器配置（给 mysql 服务使用）----
MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_DATABASE=zhineng_test_sys
MYSQL_USER=arch_radar
MYSQL_PASSWORD=change_me_user_password

# ---- Redis 容器配置（给 redis 服务使用）----
# 如果 docker-compose.yml 中启用了 Redis 密码认证
REDIS_PASSWORD=change_me_redis_password

# ---- 后端连接配置（给 backend 服务使用）----
# 注意：容器环境中，host 是 docker compose 的服务名（mysql/redis），不是 localhost
DATABASE_URL=mysql+pymysql://arch_radar:change_me_user_password@mysql:3306/zhineng_test_sys
REDIS_URL=redis://:change_me_redis_password@redis:6379/0

# ---- AI 配置（二选一）----
AI_PROVIDER=qwen
QWEN_API_KEY=your_qwen_api_key_here
# AI_PROVIDER=gemini
# GEMINI_API_KEY=your_gemini_api_key_here

# 可选：HTTP 代理（容器中访问宿主机代理）
# PROXY_URL=http://host.docker.internal:7890

# ---- 管理员账号配置（给 backend 服务使用）----
# 后端首次启动时自动创建，请务必修改默认密码
ADMIN_DEFAULT_USERNAME=admin
ADMIN_DEFAULT_PASSWORD=请设置强密码并修改此处

# ---- 前端配置（给 frontend 服务使用）----
# Vite 构建时注入的 API 地址
# 生产环境通常通过 Nginx 反代 /api，因此使用相对路径即可
VITE_API_BASE_URL=/

# 管理后台路由路径（用于隐藏真实的管理后台入口）
# 建议使用难以猜测的随机路径，例如：/sys-manage-88fa
VITE_ADMIN_PATH=/admin-secret

# 可选：运行环境标识
# APP_ENV=docker
