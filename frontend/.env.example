# =========================
# 前端开发环境变量示例（Vite）
# =========================
# 使用方法：cp frontend/.env.example frontend/.env
#
# 注意：frontend/.env 属于本地私密配置文件，禁止提交到 Git。

# 后端 API 地址（本地开发默认 http://localhost:8000）
VITE_API_BASE_URL=http://localhost:8000

# 管理后台路由前缀（可选；默认 /admin-secret）
# VITE_ADMIN_PATH=/admin-secret

