# =========================
# 本地开发环境（uvicorn 直跑）
# =========================
# 使用方法：cp .env.example .env
# 适用场景：本地开发，直接运行 uvicorn backend.main:app --reload

# 数据库配置（连接本机 MySQL）
DATABASE_URL=mysql+pymysql://root:your_password@localhost:3306/zhineng_test_sys

# Redis 配置（连接本机 Redis）
REDIS_URL=redis://localhost:6379/0

# AI 配置（二选一）
AI_PROVIDER=qwen
QWEN_API_KEY=your_qwen_api_key_here
# AI_PROVIDER=gemini
# GEMINI_API_KEY=your_gemini_api_key_here

# 可选：HTTP 代理（用于 AI API 调用，如需科学上网）
# PROXY_URL=http://127.0.0.1:7890

# 管理员账号配置（首次启动时自动创建）
# ADMIN_DEFAULT_USERNAME=admin
# ADMIN_DEFAULT_PASSWORD=请设置强密码

# 可选：运行环境标识
# APP_ENV=local
